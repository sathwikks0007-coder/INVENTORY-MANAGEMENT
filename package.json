{
  "name": "inventory-billing-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "npm install --prefix backend && npm install --prefix frontend && node ./frontend/node_modules/vite/bin/vite.js build",
    "build": "npm install --prefix backend && npm install --prefix frontend && node ./frontend/node_modules/vite/bin/vite.js build",
    "start": "node backend/server.js"
  }
}
